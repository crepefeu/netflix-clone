App\Entity\User:
  user_{1..10}:
    username: '<username()>'
    email: '<email()>'
    password: '<password()>'
    accountStatus: '<randomElement(["active", "banned", "pending", "blocked", "deleted"])>'